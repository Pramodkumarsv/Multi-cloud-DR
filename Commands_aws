ubuntu@ip-172-31-31-253:~/Multi-cloud-DR/aws$ history

    2  sudo apt update
    3  sudo snap install terraform --classic
    4  git clone https://github.com/Pramodkumarsv/Multi-cloud-DR.git


    8  terraform init
    9  terraform apply -auto-approve
   10  cd ..
   11  ls -l ~/.ssh/id_rsa.pub
   12  ssh-keygen -t rsa -b 4096 -C "pramodkumarvagannanavar@gmail.com"
   13  ls -l ~/.ssh/id_rsa ~/.ssh/id_rsa.pub
   14  terraform init
   15  cd Multi-cloud-DR/
   16  cd aws/
   17  terraform init
   18  terraform apply -auto-approve
   19  aws configure
   20  sudo apt  install awscli
   21  sudo apt update && sudo apt upgrade -y
   22  curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
   23  unzip awscliv2.zip
   24  sudo apt install unzip
   25  unzip awscliv2.zip
   26  sudo ./aws/install
   27  curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
   28  unzip awscliv2.zip
   29  sudo ./aws/install
   30  aws --version
   31  terraform init
   32  terraform apply -auto-approve
   33  cd Multi-cloud-DR/
   34  cd aws/
   35  terraform apply -auto-approve
   36  aws configure
   37  aws sts get-caller-identity
   38  terraform apply -auto-approve
   39  aws ec2 delete-key-pair --key-name "my-key"
   40  cd ..
   41  cd  ..
   42  rm Multi-cloud-DR/
   43  rmd Multi-cloud-DR/
   44  rmdir Multi-cloud-DR/
   45  rm -rf Multi-cloud-DR/
   46  ls
   47  git clone https://github.com/Pramodkumarsv/Multi-cloud-DR.git
   48  ls
   49  cd Multi-cloud-DR/
   50  cd aws
   51  terraform apply -auto-approve
   52  terraform init
   53  terraform apply -auto-approve
   54  aws ec2 describe-security-groups --query "SecurityGroups[*].[GroupName, GroupId]" --output table
   55  cd ..
   56  rm -rf Multi-cloud-DR/
   57  git clone https://github.com/Pramodkumarsv/Multi-cloud-DR.git
   58  cd Multi-cloud-DR/
   59  cd aws
   60  terraform fmt
   61  terraform init
   62  terraform apply -auto-approve
   63  aws ec2 delete-key-pair --key-name my-key1
   64  terraform state rm aws_key_pair.keypair
   65  cd ..
   66  rm -rf Multi-cloud-DR/
   67  git clone https://github.com/Pramodkumarsv/Multi-cloud-DR.git
   68  cd Multi-cloud-DR/
   69  cd aws
   70  terraform init
   71  terraform apply -auto-approve
   72  history